const firstNumber = +prompt('Enter a first number', 0);
const secondNumber = Number(prompt('Enter a second number', 0));
const result = firstNumber + secondNumber

alert(`${firstNumber}+${secondNumber}=${result}`)